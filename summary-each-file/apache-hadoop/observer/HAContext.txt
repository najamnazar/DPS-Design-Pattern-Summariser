HAContext acts as an observer for publisher FSNamesystem, which is inherited by, which sets HAState parameter of state State, which gets State, which starts Active Services, which stops Active Services, which starts Standby Services, which prepares To Stop Standby Services, which stops Standby Services, which writes Lock, which writes Unlock, which checks OperationCategory parameter of op Operation, which allows Stale Reads.
