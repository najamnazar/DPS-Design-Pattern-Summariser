RevokeFromStep acts as an abstract product for RevokeFromStep, which is inherited by RevokeImpl, RevokeImpl, RevokeImpl.
RevokeFromStep acts as an abstract factory for RevokeFinalStep, which is inherited by RevokeImpl.
