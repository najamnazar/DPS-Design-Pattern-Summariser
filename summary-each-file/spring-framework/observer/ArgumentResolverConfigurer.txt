ArgumentResolverConfigurer acts as an observer for publisher DefaultControllerSpec, which is inherited by, which adds HandlerMethodArgumentResolver... parameter of resolver Custom Resolver, which gets Custom Resolvers.
