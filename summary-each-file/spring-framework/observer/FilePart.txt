FilePart acts as an observer for publisher WebExchangeDataBinderTests, which is inherited by, which filenames, which transfers File parameter of dest To, which transfers Path parameter of dest To.
FilePart acts as an observer, which filenames, which transfers File parameter of dest To, which transfers Path parameter of dest To.
