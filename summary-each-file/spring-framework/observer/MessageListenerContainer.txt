MessageListenerContainer acts as an observer for publisher MethodJmsListenerEndpointTests, which is inherited by JmsMessageEndpointManager, MessageListenerTestContainer, AbstractMessageListenerContainer, which setups Object parameter of messageListener Message Listener, which gets Message Converter, which gets Destination Resolver, which is Pub Sub Domain, which is Reply Pub Sub Domain, which gets Reply Qos Settings.
